# don't add compiled files
bin/
obj/

# don't add files generated by premake
Makefile
*.make

# just in case someone tries to build recast manually, don't add it
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# don't add the recast svn data
.svn/

# make sure we don't check in any compiled libraries
*.a
*.lib
*.dll
*.so
*.dylib

# annoying OS-specific stuff
.DS_Store